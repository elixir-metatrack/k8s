apiVersion: apps/v1
kind: Deployment
metadata:
  name: metatrack-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: metatrack-api
  template:
    metadata:
      labels:
        app: metatrack-api
    spec:
      imagePullSecrets:
        - name: ghcr-pull-secret
      containers:
        - name: api
          image: ghcr.io/elixir-metatrack/api-server:main
          imagePullPolicy: Always
          env:
            - name: QUARKUS_HTTP_CORS
              value: "true"
            - name: QUARKUS_HTTP_CORS_ORIGINS
              value: "*"
            - name: QUARKUS_LOG_CONSOLE_JSON_ENABLED
              value: "true"
            - name: QUARKUS_LOG_CONSOLE_JSON_LOG_FORMAT
              value: "ECS"
            - name: QUARKUS_HTTP_ACCESS_LOG_ENABLED
              value: "true"
            - name: QUARKUS_LOG_CATEGORY__IO_QUARKUS_HTTP_ACCESS_LOG__LEVEL
              value: "INFO"
            # 1. Database Configuration
            - name: QUARKUS_DATASOURCE_DB_KIND
              value: "postgresql"
            - name: JDBC_STRING
              value: "jdbc:postgresql://metatrack-db:5432/metatrack"
            - name: DB_USERNAME
              value: "metatrack"
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: metatrack-postgres-secret
                  key: password
            - name: QUARKUS_HIBERNATE_ORM_SCHEMA_GENERATION
              value: "validate"
            - name: QUARKUS_FLYWAY_MIGRATE_AT_START
              value: "true"

            # 2. Keycloak / OIDC Configuration
            - name: KEYCLOAK_URL
              # Use the INTERNAL Keycloak URL for verification
              value: "https://auth.metatrack.no/realms/metatrack"
            - name: KEYCLOAK_BASE_URL
              value: "https://auth.metatrack.no"
            - name: KEYCLOAK_REALM
              value: "metatrack"
            - name: KEYCLOAK_ADMIN_CLIENT_ID
              valueFrom:
                secretKeyRef:
                 name: keycloak-admin-secret
                 key: client-id
            - name: KEYCLOAK_ADMIN_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: keycloak-admin-secret
                  key: client-secret
            - name: KEYCLOAK_USERNAME_CACHE_TTL
              value: "10M"
            - name: KEYCLOAK_USERNAME_CACHE_MAX_SIZE
              value: "1000"
            - name: QUARKUS_OIDC_CLIENT_ID
              value: "metatrack-api"

            # 3. S3 / MinIO Configuration
            - name: S3_ENDPOINT
              value: "https://storage.metatrack.no"
            - name: S3_REGION
              value: "us-east-1"
            - name: S3_PATH_STYLE_ACCESS
              value: "true"
            - name: S3_ACCESS_KEY
              value: "minio"
            - name: S3_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: minio-secret
                  key: password
            - name: S3_BUCKET_NAME
              value: "metatrack"

            # 4. App specific (Adjust based on your actual config)
            - name: TAXON_API_URL
              value: "http://taxhub"
            - name: QUARKUS_HTTP_PORT
              value: "1234"

            # 5. OpenAPI
            - name: QUARKUS_SMALLRYE_OPENAPI_PATH
              value: "/openapi"
            - name: QUARKUS_SMALLRYE_OPENAPI_ALWAYS_INCLUDE
              value: "true"

          ports:
            - containerPort: 1234

          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1024Mi"
              cpu: "500m"